# CLAUDE.md

This branch is no longer configured around Claude-specific runtime behavior.

The authoritative Codex instructions now live in `AGENTS.md` and the workflow documents in `docs/codex-workflows/`.

`.claude/` is retained as legacy reference material only.
