# Run Cards

Create a run card before any MATLAB, Julia, Quarto, LaTeX, or data-generation
job expected to take more than 5 minutes or to overwrite important outputs.

The run card is the contract for the run. If inputs, model specification, or
target outputs change, update the run card before rerunning.
